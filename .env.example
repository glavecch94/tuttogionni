# Database
DB_NAME=tuttogionni
DB_USERNAME=postgres
DB_PASSWORD=change_me

# JWT - genera un secret sicuro con: openssl rand -base64 64
JWT_SECRET=change_me

# Dominio pubblico (usato da Caddy e CORS in produzione)
DOMAIN=tuodominio.com
